import React from 'react';
import DereGraph from './graph/DereGraph';
import styled from '@emotion/styled';

// components
export const DeleGraph = () => (
  <Root>
    <DereGraph />
    hogehoge
  </Root>
);
const Root = styled.div``;

export default DeleGraph;
